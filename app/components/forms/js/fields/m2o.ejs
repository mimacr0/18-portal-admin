const <%= field.varName %>Reload = async () => {
    const res = await jsonGet('<%= field.url %>')
    if(!res?.html) return
    $('#<%= field.id %>').html(res.html)
}

$('#<%= field.id %>').select2({
    dropdownParent: $('#<%= form.varName %>Modal')
})
