<% if(item.notesText.trim().length > 0) { %>
<span class="btn btn-tool btn-sm action-id" data-id="<%= item.id %>" data-notes="<%= item.notes %>">
    <i class="fa fa-sticky-note"></i>
</span>
<% } %>
