<div <%- field.groupHTML() %>>
    ##<% if (field.label) { %>
    <label for="<%= field.id %>"><%= field.label %></label>
    ##<% } %>
    <input <%- field.inputHTML({placeholder: field.placeholder, type}) %>/>
    ##<% if (field.required) { %>
        <div class="invalid-feedback">field.required.text</div>
    ##<% } %>
</div>
