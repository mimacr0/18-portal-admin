<div <%- field.groupHTML() %>>
    ##<% if (field.label) { %>
    <label for="<%= field.id %>"><%= page.translate(field.label) %></label>
    ##<% } %>
    <input <%- field.inputHTML({placeholder: page.translate(field.placeholder), type}) %>/>
    ##<% if (field.required) { %>
        <div class="invalid-feedback">field.required.text</div>
    ##<% } %>
</div>
